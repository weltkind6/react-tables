{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "runtime": "nodejs14.x",
        "env": {
          "NODE_ENV": "production"
        },
        "command": "npm run dev"
      }
    }
  ]
}
